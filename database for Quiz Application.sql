create database	quizApp	;
use quizApp;
create	table login (name varchar(20),password varchar(20),phone varchar(20));
select	* from login;
create table score (name varchar(20),score varchar(20));
select * from score;
